# فحص أداء الموقع — تقرير ما بعد التنفيذ

**التاريخ:** 5 أغسطس 2026
**المشروع:** dogethertech.com — Next.js 15.5.19
**حالة البناء:** ✅ ناجح — 227 صفحة، صفر أخطاء، TypeScript سليم

> **ملاحظة على المنهجية:** كل رقم هنا مقاس من الملفات ومن مخرجات البناء الفعلية.
> لم أشغّل Lighthouse ولا قِسْت FPS — ده محتاج يتشغّل على جهاز حقيقي بمتصفح حقيقي.
> يعني: أقدر أقولك بالضبط كام بايت اتوفر، مش أقدر أقولك الرقم النهائي في Lighthouse.

---

## المُنفَّذ

### 1. الـ favicons — أكبر مكسب في الموقع كله

الملفات التلاتة كانت **نسخة طبق الأصل من بعض** (نفس الـ md5): صورة 1254×1254 بحجم 434KB،
واحدة اسمها `16x16` وواحدة اسمها `32x32`. و `layout.tsx` معلن التلاتة، فالمتصفح كان بينزّلهم
كلهم — بأولوية عالية في الـ `<head>`، يعني بيزاحموا المحتوى الحقيقي على الشبكة.

| الملف | قبل | بعد |
|---|---|---|
| `DoGehter-icon.png` | 434 KB @ 1254px | **56 KB** @ 512px |
| `DoGehter-32x32.png` | 434 KB @ 1254px | **1.7 KB** @ 32px |
| `DoGehter-16x16.png` | 434 KB @ 1254px | **0.8 KB** @ 16px |
| `DoGehter-192x192.png` | — | 9 KB @ 192px |
| `DoGehter-512x512.png` | — | 56 KB @ 512px |

**1.30 MB ← 59 KB على كل صفحة (−95%)**

---

### 2. أيقونات الـ PWA كانت بتدي 404

`site.webmanifest` كان بيشاور على `/android-chrome-192x192.png` و `/android-chrome-512x512.png`
— **الملفين مش موجودين أصلاً**. الأسماء الحقيقية `DoGehter-192x192.png` و `DoGehter-512x512.png`.
اتصلّحت، وأُضيف `start_url` و `lang: ar` و `dir: rtl` اللي كانوا ناقصين.

---

### 3. ملف الموديل بقى مكاش

`next.config.js` كان بيدي `immutable` للصور و `_next/static` بس. الموديل — أكبر أصل ثابت
في الموقع — كان بيقع بره القاعدة، يعني كل زائر راجع ينزّله من الأول.

```diff
- source: '/(.*)\\.(png|jpg|jpeg|gif|svg|ico|webp|avif)'
+ source: '/(.*)\\.(png|jpg|jpeg|gif|svg|ico|webp|avif|glb|gltf|bin)'
```

**1.35 MB أقل على كل زيارة متكررة**

---

### 4. `og-image` — 608 KB ← 122 KB

اتحوّل لـ JPEG بجودة 92 من غير chroma subsampling (عشان الحروف العربية تفضل حادّة).
بيأثر على سرعة معاينة الروابط في واتساب وفيسبوك، مش على سرعة الموقع نفسه.

---

### 5. شعارات المشاريع — 2.13 MB ← 150 KB

دي مكنتش في التقرير الأول لأني افترضت إنها بعيدة عن الوصول. **الافتراض كان غلط:**
لما المستخدم يضغط على فلتر تصنيف، الكود بيعيد بناء العرض من القائمة المفلترة
(`filteredProjects.slice(0, 5)`) — يعني **أي مشروع** ممكن يوصل للشاشة، مش الخمسة المميزين بس.

| الملف | قبل | بعد |
|---|---|---|
| `sharm-kitesurf.png` | 1.44 MB @ 1254px | **58 KB** webp @ 640px |
| `logo5.png` | 457 KB @ 1080px | **39 KB** webp |
| `logo1.png` | 146 KB | **23 KB** webp |
| `logo4.png` | 51 KB | **18 KB** webp |
| `logo3.png` | 38 KB | **13 KB** webp |

الشعار بيترسم على 40% من لوحة عرضها 1100 بكسل — يعني ~440 بكسل كحد أقصى. الأصول
كانت بتحمل أضعاف البكسلات اللي ممكن تظهر.

---

### 6. مستمع الـ scroll في الـ Navbar بقى `passive`

من غير `passive` المتصفح لازم يستنى الدالة تخلص عشان يعرف هل هتلغي الحركة ولا لأ،
وده بيتحسّ كتأخير في السحب على الموبايل. الدالة بتقرأ `scrollY` بس، فالوعد آمن.

---

### 7. ملفات ميتة — 4.3 MB اتشالوا

مكانتش بتتحمّل من الزوار، بس كانت بتكبّر الـ repo والنشر:

`project-logos/tripyramids.png` (1.2MB) · `DoGehter-icon-transparent.png` (1.1MB) ·
`logo2.png` (652KB) · `amjad-estate-hero.png` (106KB) · `cashier-pos.png` ·
`amjad-estate.png` · `Testimonials.jsx` (مكوّن كامل مش مستورد) · `index.css` ·
`utils/logoFallback.js` · `App.css` · `public/index.html` + `public/manifest.json`
(بقايا Create React App بتشاور على ملفات مش موجودة) · `index.html.backup` ·
مجلدات `.next.stale-backup` و `.next.failed-build-*`

وأُضيف `/.next.*` للـ `.gitignore` عشان متتراكمش تاني.

---

## قسم "أعمالنا المميزة" (من الجلسة الأولى)

| البند | قبل | بعد |
|---|---|---|
| شعارات بتتحمّل وبتترمي | ~1.5 MB | **0** — الشعار بيتحمّل بس لو الـ screenshot فشل |
| موديل الـ GLB | 2.07 MB | **1.35 MB** — textures لـ WebP + weld/quantize |
| شعار الزاوي الجديد | 652 KB | **70 KB** |
| قياس الـ layout | 6 × `getBoundingClientRect` كل حدث scroll | مخزّن، بيتقاس عند التغيير بس |
| رسم لوحات الشاشة | 5 لوحات 1100px في task واحدة | واحدة واحدة، و640px على الموبايل |
| MSAA | شغّال دايماً | مقفول على الشاشات ≥1.5 DPR |

---

## الإجمالي

| | التوفير |
|---|---|
| **على كل صفحة، كل زائر** | **~1.24 MB** (الأيقونات) |
| **قسم الأعمال، أول زيارة** | **~2.3 MB** |
| **الزيارات المتكررة** | **1.35 MB** إضافية (كاش الموديل) |
| **حجم النشر** | **~6.5 MB** |

`.next/static/media` نزل من **2.7 MB لـ 776 KB**.

---

## ✅ الحاجات اللي طلعت سليمة

- **حجم الـ JS محترم:** 103KB مشترك، الصفحة الرئيسية 190KB
- **three.js متأجّل بالكامل:** ~1MB خام في chunks منفصلة، بتتحمّل لما القسم يقرب بس
- **الـ 227 صفحة كلها prerendered** رغم إن المكوّنات `'use client'`
- **الـ Analytics متأخر صح:** Clarity `afterInteractive`، Meta Pixel `lazyOnload`
- **الـ CSS 64KB** لكل الموقع

---

## متبقّي — محتاج قرارك

### الخطوط (~85 KB على كل صفحة)

الموقع بينزّل Inter و Poppins، الاتنين `subsets: ['latin']` — يعني **حروف إنجليزية بس**.
والموقع `lang="ar"`. النص العربي بيتعرض بخط النظام لأن الخطين مفيهمش حروف عربية أصلاً.

الخطوط دي بتخدم الأرقام وكلمات زي "POS" و "TriPyramids" بس، و `next/font` بيعمل لهم
preload تلقائي على كل صفحة.

**السؤال:** هل الشكل اللي بيدوه للكلمات الإنجليزية يستاهل 85KB؟ ده قرار تصميمي، مش تقني.
الخيارات: الاكتفاء بخط واحد · شيلهم خالص · أو استبدالهم بخط عربي حقيقي لو التصميم محتاجه.

### حاجة تانية لاحظتها — وميض اللغة والثيم

`page.tsx` بيبدأ بـ `lang='ar'` و `darkMode=true` كقيم افتراضية، وبعدين بيقرأ الاختيار
الحقيقي من `localStorage` جوه `useEffect` — يعني **بعد** أول رسم. لو زائر مختار الوضع
الفاتح أو الإنجليزي، هيشوف ومضة من الوضع الغلط الأول.

ده مش بطء بالمعنى الحرفي، لكنه بيتحسّ كبطء وبيأثر على مقياس CLS في Lighthouse.
الحل المعتاد سكريبت صغير `beforeInteractive` في الـ `<head>` بيقرأ `localStorage`
ويحط الكلاس قبل ما React يشتغل. شغل متوسط — قوللي لو عايزه.
